package {{base_package}};

import {{base_package}}.item.ModItems;
import {{base_package}}.block.ModBlocks;
import {{base_package}}.Config;
import net.minecraft.world.item.CreativeModeTabs;
import org.slf4j.Logger;
import com.mojang.logging.LogUtils;

import net.neoforged.api.distmarker.Dist;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.ModContainer;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.fml.common.Mod;
import net.neoforged.fml.config.ModConfig;
import net.neoforged.fml.event.lifecycle.FMLClientSetupEvent;
import net.neoforged.fml.event.lifecycle.FMLCommonSetupEvent;
import net.neoforged.neoforge.common.NeoForge;
import net.neoforged.neoforge.event.BuildCreativeModeTabContentsEvent;
import net.neoforged.neoforge.event.server.ServerStartingEvent;

// The value here should match an entry in META-INF/neoforge.mods.toml
@Mod({{main_class_name}}.MOD_ID)
public class {{main_class_name}} {
    public static final String MOD_ID = "{{modid}}";
    private static final Logger LOGGER = LogUtils.getLogger();

    // FML will inject these constructor params
    public {{main_class_name}}(IEventBus modEventBus, ModContainer modContainer) {
        // Register lifecycle listeners
        modEventBus.addListener(this::commonSetup);

        // Register this class on the global event bus (only if you have @SubscribeEvent methods)
        NeoForge.EVENT_BUS.register(this);

        // Register item registry
        ModItems.register(modEventBus);

        // Register block registry
        ModBlocks.register(modEventBus);

        // Creative tab population
        modEventBus.addListener(this::addCreative);

        // Optional config registration (keep if you have a Config class)
        modContainer.registerConfig(ModConfig.Type.COMMON, Config.SPEC);
    }

    private void commonSetup(final FMLCommonSetupEvent event) {}

    private void addCreative(BuildCreativeModeTabContentsEvent event) {
        // minecraft:ingredients
        if (event.getTabKey() == CreativeModeTabs.INGREDIENTS) {
            // ==minecraft:ingredients_ACCEPT_BEGIN==
            // event.accept(ModItems.EXAMPLE_INGOT);
            // ==minecraft:ingredients_ACCEPT_END==
        }

        // minecraft:building_blocks
        if (event.getTabKey() == CreativeModeTabs.BUILDING_BLOCKS) {
            // ==minecraft:building_blocks_ACCEPT_BEGIN==
            // event.accept(ModBlocks.EXAMPLE_BLOCK);
            // ==minecraft:building_blocks_ACCEPT_END==
        }

        // minecraft:colored_blocks
        if (event.getTabKey() == CreativeModeTabs.COLORED_BLOCKS) {
            // ==minecraft:colored_blocks_ACCEPT_BEGIN==
            // event.accept(ModBlocks.EXAMPLE_WOOL);
            // ==minecraft:colored_blocks_ACCEPT_END==
        }

        // minecraft:natural_blocks
        if (event.getTabKey() == CreativeModeTabs.NATURAL_BLOCKS) {
            // ==minecraft:natural_blocks_ACCEPT_BEGIN==
            // event.accept(ModBlocks.EXAMPLE_ORE);
            // ==minecraft:natural_blocks_ACCEPT_END==
        }

        // minecraft:functional_blocks
        if (event.getTabKey() == CreativeModeTabs.FUNCTIONAL_BLOCKS) {
            // ==minecraft:functional_blocks_ACCEPT_BEGIN==
            // event.accept(ModBlocks.EXAMPLE_CRAFTING_STATION);
            // ==minecraft:functional_blocks_ACCEPT_END==
        }

        // minecraft:redstone_blocks
        if (event.getTabKey() == CreativeModeTabs.REDSTONE_BLOCKS) {
            // ==minecraft:redstone_blocks_ACCEPT_BEGIN==
            // event.accept(ModBlocks.EXAMPLE_REDSTONE_GADGET);
            // ==minecraft:redstone_blocks_ACCEPT_END==
        }

        // minecraft:tools_and_utilities
        if (event.getTabKey() == CreativeModeTabs.TOOLS_AND_UTILITIES) {
            // ==minecraft:tools_and_utilities_ACCEPT_BEGIN==
            // event.accept(ModItems.EXAMPLE_TOOL);
            // ==minecraft:tools_and_utilities_ACCEPT_END==
        }

        // minecraft:combat
        if (event.getTabKey() == CreativeModeTabs.COMBAT) {
            // ==minecraft:combat_ACCEPT_BEGIN==
            // event.accept(ModItems.EXAMPLE_SWORD);
            // ==minecraft:combat_ACCEPT_END==
        }

        // minecraft:food_and_drinks
        if (event.getTabKey() == CreativeModeTabs.FOOD_AND_DRINKS) {
            // ==minecraft:food_and_drinks_ACCEPT_BEGIN==
            // event.accept(ModItems.EXAMPLE_FOOD);
            // ==minecraft:food_and_drinks_ACCEPT_END==
        }

        // minecraft:spawn_eggs
        if (event.getTabKey() == CreativeModeTabs.SPAWN_EGGS) {
            // ==minecraft:spawn_eggs_ACCEPT_BEGIN==
            // event.accept(ModItems.EXAMPLE_SPAWN_EGG);
            // ==minecraft:spawn_eggs_ACCEPT_END==
        }
    }

    @SubscribeEvent
    public void onServerStarting(ServerStartingEvent event) {}

    @EventBusSubscriber(modid = MOD_ID, bus = EventBusSubscriber.Bus.MOD, value = Dist.CLIENT)
    public static class ClientModEvents {
        @SubscribeEvent
        public static void onClientSetup(FMLClientSetupEvent event) {}
    }
}
