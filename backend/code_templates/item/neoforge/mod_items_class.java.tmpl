package {{base_package}}.item;

import {{base_package}}.{{main_class_name}};
import net.minecraft.world.item.Item;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.neoforge.registries.DeferredItem;
import net.neoforged.neoforge.registries.DeferredRegister;
import net.minecraft.world.item.Rarity;
import net.minecraft.world.food.FoodProperties;
import net.minecraft.world.effect.MobEffectInstance;
import net.minecraft.world.effect.MobEffects;

// ==MM:EXTRA_IMPORTS_BEGIN==
// ==MM:EXTRA_IMPORTS_END==

public class ModItems {
    public static final DeferredRegister.Items ITEMS = DeferredRegister.createItems({{main_class_name}}.MOD_ID);

    // ==MM:ITEM_REGISTRATIONS_BEGIN==
    // ==MM:ITEM_REGISTRATIONS_END==

    public static void register(IEventBus eventBus) {
        ITEMS.register(eventBus);
    }
}
