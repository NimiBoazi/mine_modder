package {{base_package}}.block;

import {{base_package}}.{{main_class_name}};
import {{base_package}}.item.ModItems;
import net.minecraft.world.item.BlockItem;
import net.minecraft.world.item.Item;
import net.minecraft.world.level.block.Block;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.neoforge.registries.DeferredBlock;
import net.neoforged.neoforge.registries.DeferredRegister;
import net.minecraft.world.level.block.*; // StairBlock, SlabBlock, DoorBlock, etc.
import net.minecraft.world.level.block.state.BlockBehaviour;
import net.minecraft.world.level.block.state.properties.BlockSetType;
import net.minecraft.world.level.block.state.properties.WoodType;
import net.minecraft.util.valueproviders.UniformInt;

// ==MM:EXTRA_IMPORTS_BEGIN==
// ==MM:EXTRA_IMPORTS_END==

import java.util.function.Supplier;

public class ModBlocks {
    public static final DeferredRegister.Blocks BLOCKS =
            DeferredRegister.createBlocks({{main_class_name}}.MOD_ID);

    // ==MM:BLOCK_REGISTRATIONS_BEGIN==
    // Examples:
    //
    // public static final DeferredBlock<Block> EXAMPLE_BLOCK = registerBlock("example_block",
    //     () -> new Block(BlockBehaviour.Properties.of().strength(4f)));
    //
    // public static final DeferredBlock<Block> EXAMPLE_ORE = registerBlock("example_ore",
    //     () -> new DropExperienceBlock(
    //         UniformInt.of(2, 4),
    //         BlockBehaviour.Properties.of().strength(3f)
    //     ));
    //
    // public static final DeferredBlock<StairBlock> EXAMPLE_STAIRS = registerBlock("example_stairs",
    //     () -> new StairBlock(EXAMPLE_BLOCK.get().defaultBlockState(),
    //         BlockBehaviour.Properties.of().strength(2f)));
    //
    // public static final DeferredBlock<DoorBlock> EXAMPLE_DOOR = registerBlock("example_door",
    //     () -> new DoorBlock(BlockSetType.IRON,
    //         BlockBehaviour.Properties.of().strength(2f).noOcclusion()));
    //
    // // Custom block classes are fine here too:
    // // public static final DeferredBlock<Block> EXAMPLE_CUSTOM = registerBlock("example_custom",
    // //     () -> new ExampleCustomBlock(BlockBehaviour.Properties.of().strength(2f)));
    // ==MM:BLOCK_REGISTRATIONS_END==

    private static <T extends Block> DeferredBlock<T> registerBlock(String name, Supplier<T> block) {
        DeferredBlock<T> toReturn = BLOCKS.register(name, block);
        registerBlockItem(name, toReturn);
        return toReturn;
    }

    private static <T extends Block> void registerBlockItem(String name, DeferredBlock<T> block) {
        ModItems.ITEMS.register(name, () -> new BlockItem(block.get(), new Item.Properties()));
    }

    public static void register(IEventBus eventBus) {
        BLOCKS.register(eventBus);
    }
}
