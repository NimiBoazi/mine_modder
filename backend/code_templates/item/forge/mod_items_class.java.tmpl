package {{base_package}}.item;

import {{base_package}}.{{main_class_name}};
import net.minecraft.world.item.Item;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.registries.DeferredRegister;
import net.minecraftforge.registries.ForgeRegistries;
import net.minecraftforge.registries.RegistryObject;

public class ModItems {
    public static final DeferredRegister<Item> ITEMS =
            DeferredRegister.create(ForgeRegistries.ITEMS, {{main_class_name}}.MOD_ID);

    // ==MM:ITEM_REGISTRATIONS_BEGIN==
    // Subgraph inserts one or more blocks like:
    // public static final RegistryObject<Item> {{registry_constant}} = ITEMS.register("{{item_id}}",
    //         () -> new Item(new Item.Properties()));
    // ==MM:ITEM_REGISTRATIONS_END==

    public static void register(IEventBus eventBus) {
        ITEMS.register(eventBus);
    }
}